.. Shows how versioning is controlled on the website.

Versioning
==========

The latest version is available at the top of :doc:`release_notes` as uses the
`Semantic Versioning 2.0.0`_ schema:

Given a version number MAJOR.MINOR.PATCH, increment the:

* ``MAJOR`` - Incompatible API changes,
* ``MINOR`` - Added functionality in a backwards compatible manner.
* ``PATCH`` - Backwards compatible bug fixes.

Additional labels for pre-release and build metadata are available as extensions to the ``MAJOR.MINOR.PATCH`` format.

.. _`Semantic Versioning 2.0.0`: https://semver.org/